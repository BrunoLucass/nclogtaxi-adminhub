# NC LOG Middleware API (backend) — sem barra no final; health: GET /health
VITE_API_BASE_URL=https://nctaxi-middleware.onrender.com

# Supabase — Project Settings → API → anon public key (used as apikey header on POST /auth/login)
VITE_SUPABASE_ANON_KEY=


